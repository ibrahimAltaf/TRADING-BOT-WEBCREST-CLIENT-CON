# Build the dashboard for production (run from trading-bot-dashboard/)
#   cp production.env.example .env.production
#   npm run build
#
# Point to your public API (same host + path, or subdomain e.g. https://api.yourdomain.com)

VITE_API_BASE_URL=https://api.yourdomain.com
