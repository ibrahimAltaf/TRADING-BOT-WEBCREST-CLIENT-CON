# Production / VPS dashboard build — copy to .env.production before `npm run build`
# Same-origin as the site: nginx serves UI and proxies /api → backend :8000
VITE_API_BASE_URL=/api

# Or full API URL (no trailing slash on domain):
# VITE_API_BASE_URL=https://your-domain.com/api
